name: .NET

on:
  push:
    branches:
    - main
    - develop
  pull_request:
    branches: 
    - main
    - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.*
    - name: Restore dependencies
      run: dotnet restore ./src/GithubPoc
    - name: Build
      run: dotnet build ./src/GithubPoc --no-restore
      
  test:
    name: Test on ${{ matrix.os }}
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
    - name: Test on ${{ matrix.os }}
      run: dotnet test "${{ github.workspace }}/src/GithubPocUnitTests" --no-build --verbosity normal
      
  debug:
    needs: test
    runs-on: ubuntu-latest
    if: ${{ failure() }}
    steps:
    - run: echo ${{ github.workspace }}
    - name: debug
      run: ls
    - name: debug2
      run: printenv
    
